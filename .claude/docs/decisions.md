# Decisions Log

Decisions that shape later work. Not a changelog — only things that would be expensive to
reverse or that a future session might otherwise re-litigate.

| ID | Date | Decision | Status | Rationale | Affects |
|---|---|---|---|---|---|
| D1 | 2026-09-05 | Genericize Supabase source-side names; preserve the return contract the Rmd expects | Decided | Interview demo doesn't need real internal table/column names, but downstream code depends on the column names the pull functions return | `R/pull_apps.R`, `R/function_cc_scorecard_data.R`, Supabase table design |
| D2 | 2026-09-05 | Postgres via Supabase replaces DB2/Oracle warehouse; `DECODE` → `CASE`, etc. | Decided | No access to production databases outside work environment | `R/pull_apps.R`, `R/function_cc_scorecard_data.R`, all sourced scripts |
| D3 | 2026-09-05 | Environment credentials move from `keyring` to `.Renviron` via `Sys.getenv()` | Decided | Supabase uses URL + API key, not enterprise keyring; simpler for demo | `R/pull_apps.R`, `R/function_cc_scorecard_data.R`, `.Renviron` |
| D4 | 2026-09-05 | Three-pass gated workflow with user approval at each boundary | Decided | Prevents compounding errors from OCR transcription and context loss across sessions | `.claude/skills/`, all task execution |
| D5 | 2026-09-05 | Reviewed ≠ validated: code is validated only once it has run | Decided | Two errors survived past the QC marker because they were syntactically plausible | `orchestration_2.Rmd` QC marker, Pass 3 rule 3 |
| D6 | 2026-09-05 | Sample data generator takes drift parameters so metrics can be shown responding | Decided | PSI/CSI/KS need visible movement to demonstrate the monitoring pipeline works | Future data generation scripts, Supabase seed data |
| D7 | 2026-09-05 | Supabase apps source is a SINGLE FLAT TABLE; five joined warehouse tables (ADM_APP_INFO, ADM_GEN_VAL, ADM_DATA_ELEM, ADM_TS2_EXTR_OVR, ADM_APP_LOG) collapse to one `applications` table. No EAV, no pivot, no subqueries. `get_apps_data()` becomes a single SELECT with column aliases and a date filter. Scorecard already flat. Preserve the full output column list, including columns with zero current uses in the Rmd. | Decided | Warehouse schema complexity is an artifact of the source system, not a requirement of the monitoring pipeline. Flat table is simpler to seed, query, and maintain for demo purposes. Unused columns are retained because the Rmd regions that would consume them (lines 240-445; anchor: :240 = narrative after QC marker) carry the heaviest OCR damage — "unused" there means "unreadable", not "absent". Adding a column later is trivial; discovering a missing one while rehearsing is not. Acknowledged cost: going flat gives up the DECODE -> MAX(CASE WHEN) dialect conversion as a live walkthrough example; it survives only in the reconstruction diff. | `R/pull_apps.R`, `R/function_cc_scorecard_data.R`, Supabase table design, schema task |
| D8 | 2026-09-06 | prim_score is a SCORE VALUE in the range 100-450, not a scorecard identifier. Resolved by user from the original screenshots. | Decided | The element name PRIM_SCORE_CARD is misleading; the Rmd's numeric comparisons (< 100 ~ -1, > 450 ~ 9999, >= 100 filter) are correct. Type it numeric in Postgres: the source pulls trim(ALPHA_VALUE) as TEXT, and if fread types the column character, prim_score >= 100 becomes a lexicographic comparison where "99" >= "100" is TRUE — silently admitting out-of-range rows to the PSI population with no error. | Supabase schema, `R/pull_apps.R`, round-trip test |
| D9 | 2026-09-06 | Scorecard table grain: one row per user_ref_num (1:1 with the apps join). sq_num retained as a column per D7 but is not grain-defining. | Decided | We design the table, so grain is a choice. The Rmd's left_join() has no by= clause, so fan-out would be invisible. The interesting divergence between n_distinct(app_num) and n_distinct(user_ref_num) already comes from the copied_from remapping; a second source would obscure it. | Supabase schema, sample-data generator |
| D10 | 2026-09-06 | Scorecard column `priored1` is actually `primemdt`. Resolved by user review of the original source. Typed TEXT. | Decided | Note that primemdt matches NEITHER OCR candidate — discrepancies.log:43 offered primedt (Version A) and priored1 (Version B, kept by the pipeline's arbitration rule). Both were wrong. The OCR pipeline's discrepancy arbitration is a forced binary choice with no "neither" branch, which is a real limitation of its design. Type left as TEXT deliberately: the name is resolved, the type is not, and the column has zero downstream uses. | `R/function_cc_scorecard_data.R`, `CLAUDE.md`, Supabase schema |
| D11 | 2026-09-06 | Cut applid, logic, custom_score, custom_score_2, custom_score_3 from the apps contract. Partially reverses D7's blanket retention rule. | Decided | Checked for CSI specifically: the CSI section (orchestration_2.Rmd:452-527; connection block replaced by D13 stub at :456-458; anchor: :452 = CSI chunk opening fence) opens its own connection to a separate BI warehouse and pulls development-population features from there — it does not consume apps columns, so D7's "unused means unreadable" rationale does not cover these five. They are warehouse artifacts with no analytical content: applid is a TS2 override join, logic is the officer-log subquery (JC1/JC2 exist only to serve it), and the custom_score columns' only interest was the EAV pivot that flattening already removed. D7's retention rule still stands for acq, applied, assigned_credit_lim, bureau_used. | `R/pull_apps.R`, Supabase schema, sample generator |
| D12 | 2026-09-06 | Cut copied_from and the entire remapping path. | Executed | EXECUTED by make-rmd-run-to-marker: QC chunk (formerly :171-210) deleted, copied_from/from_value/to_value/copied_flag stripped from psi_df (formerly :216-221). Pre-deletion ranges retained for history. Simplicity over demonstrable domain logic — deliberate trade, taken by the user. Acknowledged cost: the validated region loses its only before/after data-quality measurement; has_score / has_segment grouping remains and can carry some of that role. | `orchestration_2.Rmd`, `R/pull_apps.R`, Supabase schema, sample generator |
| D13 | 2026-09-06 | CSI (orchestration_2.Rmd:329-485; originally stub at :401-450, now fully built by build-csi-calculation/D19). Validated frontier at :635. | Executed | Originally kept the loop bounded at two tables; now all four are implemented. | Scope, `R/compute_si.R`, `orchestration_2.Rmd` CSI chunk |
| D14 | 2026-09-06 | Zero-bin epsilon policy: `psi_epsilon <- 0.0001` floors zero percentages to avoid `ln(0) = -Inf`. Named parameter, not inline magic number. | Decided | Flooring biases PSI upward; the alternative (dropping empty bins) understates drift by hiding exactly the bins where the population vanished. With 31 applications across 120 bins, 81 bins are empty on the first run — every segment hits this. Segments with zero total applications are flagged via `epsilon_only` column and a warning, since their PSI is computed entirely from the floor and is meaningless. | `orchestration_2.Rmd` PSI chunk, `R/build_dev_population.R` |
| D15 | 2026-09-06 | Frozen vigintile finding: `segment_breaks` are frozen development-population vigintile cutpoints. Uniform dev counts (5% per bin) are correct by construction, not an approximation. If re-derived each run, PSI would always be ~0. Closes the "frozen or re-derived" open question. | Decided | A population binned by its own quantiles produces equal-frequency bins. The breaks are hardcoded at `orchestration_2.Rmd:184-197` and duplicated in `R/build_dev_population.R` and `R/generate_cohort.R`. Development counts are notional (1000/bin per segment, 5000/bin for All Segments); only the percentages matter for PSI. | `R/build_dev_population.R`, `R/generate_cohort.R`, `orchestration_2.Rmd` PSI chunk |
| D16 | 2026-09-06 | Drift schedule and generator design. Per-segment PSI targets (not uniform across segments). Schedule: Q4=all flat, Q1=(0.08/0.03/0.05/0.02/0.04), Q2=(0.18/0.05/0.12/0.03/0.08), Q3=(0.30/0.09/0.25/0.04/0.15). Story: segment 0 deteriorating into action band, segment 2 following, segments 1/3/4 stable. Each segment solves its own alpha via bisection. Volumes 30,500/quarter (5,500/8,000/6,000/5,500/5,500 per segment). Allocation deterministic (round + remainder), not multinomial. Rejected approach: shuffling applications between segments post-hoc does NOT produce distinct per-segment PSIs — PSI is computed on percentages, so moving rows between segments leaves each segment's bin distribution unchanged; the only real effect comes from re-binning through different break vectors, which is uncontrollable. "All Segments" PSI diverges from per-segment values because pooled scores are re-binned through a different break vector; at alpha=0 the residual is ~0.01 (synthetic data artifact, zero in production by construction). | Decided | PSI is a deterministic function of alpha given flat dev weights. The 50-count floor caps drift: max PSI ~0.30 at N=5,500. | `R/generate_cohort.R`, `R/setup_supabase.R` |
| D18 | 2026-09-06 | Frozen feature breaks (analogous to D15 for segment_breaks). Global breaks — one per feature, not per-segment — because the generator draws features from identical distributions for all segments. Known simplification: in production, segments genuinely have different feature distributions and would carry different cutpoints. Continuous breaks are the generator's evenly-spaced bin boundaries (`seq(range[1], range[2], length.out = n_bins + 1)`), which are the development population's deciles by construction. At alpha=0, binning gives exactly uniform counts for features where n_bins divides all segment volumes (feature_1, feature_3); for feature_2 (8 bins, 5500/8 non-integer), per-segment remainder accumulation produces a ±3 count delta (0.08%) — not a pipeline error but a property of pooling per-segment deterministic allocations. All Segments CSI at Q4 2025 is ~0 for all features; feature_2's remainder residual is 1.548e-07 (seven orders of magnitude below any meaningful value). Flat-cohort self-test threshold: CSI < 1e-6 (not == 0), because feature_2's remainder makes exact zero unachievable for a correct pipeline. The 1e-6 threshold catches real breakage by a wide margin — the smallest genuine CSI target is 0.01, four orders above. The breaks live in `R/feature_breaks.R` as literal R source (diffable, greppable, reviewable), NOT as a binary RDS. `R/build_feature_breaks.R` can bootstrap the file from feature_defs if absent, and validates the committed file against feature_defs on every run — fails loudly on drift. dev_counts is the single source of truth (all five features total 10,000 — one dev population binned five ways); categorical dev_props is derived and asserted, not stored. | Decided | Same trap as PSI: if breaks are re-derived from each cohort, CSI is always ~0. File-on-disk is the freeze mechanism. | `R/feature_breaks.R`, `R/build_feature_breaks.R` |
| D19 | 2026-09-06 | CSI calculation and shared stability index helper. `R/compute_si.R` extracts the PSI formula (:282-318 pre-refactor) into `compute_stability_index(joined_df, epsilon)`, used by both PSI and CSI. Helper asserts `sum(percent_dev)==1` per Scorecard group; PSI call site keeps the tighter `all(percent_dev==0.05)` assertion. CSI joins features to psi_df via inner_join on user_ref_num (guarantees identical population). Bin index (integer 1:n) is the join key for CSI — not bin labels — eliminating the formatting-risk class. Dev reference uses the same `fb$dev_counts` for all segments including All Segments (global breaks → identical proportions). All Segments CSI at flat cohort is ~1.5e-7 (feature_2 remainder), confirming no per-segment re-binning leak (PSI's All Segments carries a ~0.01 residual from its per-segment break vectors). `all(tbl$epsilon_only)` used in Total row instead of `tbl$epsilon_only[1]` for explicit intent. xlsx: 5 tabs (one per feature), segment as column — not 30 tabs. | Executed | Single source of truth for the SI formula prevents drift between PSI and CSI. Bin-index join prevents silent all-NA joins from label formatting mismatches. | `R/compute_si.R`, `orchestration_2.Rmd` PSI chunk (:268-325), CSI chunk (:335-485) |
| D20 | 2026-09-07 | Performance table and KS/DQ90 rank ordering. Performance table (`performance`) holds approved applications only — user_ref_num PK, origination_date, dq90 (0/1). KS uses a 12-month-lagged cohort (Q3 2025 bookings for Q3 2026 reporting). Bad-rate model: `P(dq90=1\|score) = logistic(a + b*score)` solved per segment for (bad rate, KS) jointly. Segment 0 current quarter adds a Gaussian bump to break monotonicity in deciles 4-6 — this is the performance signal that pairs with segment 0's elevated PSI (0.30). Development baseline frozen to `R/ks_baseline.R` (literal R source, per D18 convention). All Segments KS expected below most per-segment values due to pooling of populations with different bad rates and score-to-risk slopes. Per-segment approval rates (85/70/60/40/55% for segments 0-4) applied to ALL quarters; `applied` column set to 1L for all rows (everyone in the table applied for credit). Decile convention: decile 1 = highest score = lowest risk; bad rates increase with decile number. Monotonicity assertion is non-strict (`>=`, ties pass). Segment 0 dev deciles 1-3 have 1, 2, 2 bads respectively — deciles 2 and 3 tie at 0.0014. This is expected: a KS of 41.8 at 2.15% bad rate means the scorecard concentrates nearly all bads in the worst deciles. The "28 bads/decile" threshold is an average (total_bads/10); the actual distribution is heavily right-skewed by design. Segment 3 dev bad rate (12.57%) exceeds the 12.0% target by 0.57pp — Bernoulli sampling variance at n=6,569 (SE=0.40pp, miss is 1.4 SE); the solver is correct, the tolerance was too tight. | Decided | Performance evidence is always 12 months behind the input distribution. KS deciles are re-derived (not frozen) — opposite of PSI/CSI. | `R/generate_cohort.R`, `R/pull_performance.R`, `R/compute_ks.R`, `R/build_ks_baseline.R`, `R/ks_baseline.R`, `sql/04_create_performance_table.sql`, `orchestration_2.Rmd` KS chunk (:495-631) |
| D17 | 2026-09-06 | Feature design and categorical dev reference. 5 features: 3 continuous (feature_1/10 bins, feature_2/8 bins, feature_3/5 bins) and 2 categorical (feature_4/4 levels A-D at 0.45/0.30/0.15/0.10, feature_5/3 levels X-Z at 0.60/0.30/0.10). Features table at D9 grain (one row per user_ref_num). Generalized solver: `psi_from_alpha`, `solve_alpha`, `tilt_weights` accept optional `dev_weights` for non-uniform dev (categorical features). For uniform q (continuous), the formula recovers the original: sum(q_i*t_i) = 0 → normalization is 1 → w_i = (1+alpha*t_i)/n. For non-uniform q, the normalization sum(q_i*(1+alpha*t_i)) != 1 in general; the code normalizes correctly. Categorical tilt direction: `tilt_dir = -1` reverses which end of the level list is boosted. For feature_4, level D (10% dev, minority) gains mass; for feature_5, level Z (10% dev, minority) gains mass. This models channel-mix drift away from the dominant source — a recognizable real-world pattern that pairs with the deteriorating score distribution (PSI story). The alternative (tilt_dir = +1, boosting the already-dominant level) would have the drift direction be an artifact of list order with no real-world analog. PSI is RNG-invariant: bin counts come from deterministic_allocate (no randomness), and integer scores with +1L boundary alignment always map back to their originating bin under cut(right=TRUE, include.lowest=TRUE). Verified empirically: seed=42 vs seed=43 produce bit-identical PSI. Feature generation uses an independent RNG stream (seed+1000) as defensive practice. CSI targets verified on the core population only (client_product_cd in CC/PLAT/GOLD, non-null prim_score, non-null segment) — noise rows carry alpha=0 and would dilute realized CSI if included. | Decided | Categorical features break two PSI assumptions: (1) quantile bins make dev uniform by construction, but categorical dev proportions are the actual level mix; (2) the solver needed generalization to accept non-uniform dev_weights. | `R/generate_cohort.R`, `R/pull_features.R`, `R/setup_supabase.R`, `sql/03_create_features_table.sql` |
